import React from "react";
import AuthRoutes from "../routes/AuthRoutes";

const Authpage = () => {
    return (
        <div>
            Auth Page
            <AuthRoutes/>
        </div>
    );
};

export default Authpage;